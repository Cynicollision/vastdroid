/// <reference path="namespace.js" />

/**
 * Stores game-level settings.
 * @class Config
 * @memberof Vastengine
 */
vastengine.Config = {
    fullScreen: false,
    gameSpeed: 60,
    scaleCenter: false,
    scale: 1,
    canvasWidth: 640,
    canvasHeight: 480,
    defaultFrameSpeed: 10
};